[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --strict-markers
markers =
    golden: locked-in reference-data calculation tests
    integration: tests that hit external services
filterwarnings =
    ignore::DeprecationWarning
